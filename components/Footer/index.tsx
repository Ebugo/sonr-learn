import React from "react";
import styles from "./styles.module.css";

const Footer = () => {
	return (
		<footer className={`${styles.container} pt-12 pb-20`}>
			Footer
		</footer>
	);
};

export { Footer };
